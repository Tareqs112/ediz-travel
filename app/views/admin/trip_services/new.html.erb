<% content_for :title, "Add Service — Booking ##{@booking.id}" %>

<div class="mb-6">
  <%= link_to "← Back to Booking", admin_booking_path(@booking), class: "text-sm text-gray-500 hover:text-gray-900" %>
</div>

<div class="max-w-4xl">
  <%= render "form" %>
</div>
